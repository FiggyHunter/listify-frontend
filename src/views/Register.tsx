import { Link } from "react-router-dom";
import "../App.css";

const Login = () => {
  return (
    <main className="">
      Wow, azure! <Link to="/login">Login</Link>
    </main>
  );
};

export default Login;
